Train:
  batch_size: 128
  learning_rate: 0.05
  reg: 0.00005
  epochs: 15
  steps: [6, 8]
  warmup: 0
  momentum: 0.9
  embed_dim: 1024
  num_classes: 1048

network:
  model: im2recipe

data:
  save_best: True
